import DeviceActivity

extension DeviceActivityName {
    static var commitmentScreenTime: Self { Self("commitmentScreenTime") }
}

extension DeviceActivityEvent.Name {
    static var screenTimeLimit: Self { Self("screenTimeLimit") }
}
